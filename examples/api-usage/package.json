{
  "name": "api-usage",
  "version": "1.0.0",
  "description": "SVGInjector API Usage Example",
  "scripts": {
    "build": "parcel build index.html",
    "prebuild": "npm run copysvg",
    "prestart": "npm run copysvg",
    "start": "parcel index.html --open",
    "copysvg": "mkdir -p dist && cp *.svg dist"
  },
  "dependencies": {
    "@tanem/svg-injector": "latest"
  },
  "keywords": [
    "@tanem/svg-injector"
  ],
  "devDependencies": {
    "parcel": "2.8.0"
  }
}
