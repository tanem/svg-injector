{
  "name": "mootools",
  "version": "1.0.0",
  "description": "SVGInjector MooTools Example",
  "scripts": {
    "build": "parcel build index.html",
    "prebuild": "npm run copysvg",
    "prestart": "npm run copysvg",
    "start": "parcel index.html --open",
    "copysvg": "mkdir -p dist && cp *.svg dist"
  },
  "dependencies": {
    "@tanem/svg-injector": "latest"
  },
  "devDependencies": {
    "parcel": "2.16.4"
  },
  "keywords": [
    "@tanem/svg-injector"
  ]
}
